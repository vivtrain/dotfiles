# Initial Options ##############################################################
#set  -g  default-terminal "screen-256color-bce"
set  -g  terminal-overrides ",*:RGB"
set  -g  status-bg 'blue'
set  -g  status-fg 'white'
set  -g  base-index 1
setw -g  pane-base-index 1
set  -g  mouse off
setw -g  mode-keys vi
set  -sg escape-time 50 # need some delay for Windows Terminal Ubuntu 20.04
set  -g  status-right "%A, %B %d, %Y, %I:%M %P"

set-option -g display-time 5000

set-option -g pane-border-style "fg=#505050"
set-option -g pane-active-border-style "fg=#4080ff"
set-option -g pane-border-format " #{pane_index}: #{pane_current_command} "
set-window-option -g window-status-current-style "bg=#80aaff,fg=#081f4d"

set -g focus-events on
set-hook -g pane-focus-in "set-option -gFw pane-border-status \"#{?#{e|>:#{window_panes},1},top,off}\""

# Comand Aliases ###############################################################

# Maps #########################################################################
set -g repeat-time 0

# rebind prefix
unbind C-b
set -g prefix M-a
bind M-a send-prefix

bind-key -n M-\; command-prompt

# source this config file (for easy adjustments without quitting tmux)
bind -n M-r "source-file ~/.tmux.conf\; display 'Reloaded ~/.tmux.conf'"

# Vim copy
bind-key -T copy-mode-vi v send-keys -X begin-selection
bind-key -T copy-mode-vi Escape send-keys -X cancel
bind-key -T copy-mode-vi y send-keys -X copy-selection
bind-key -T copy-mode-vi c-v \
  send-keys -X rectangle-toggle \; send-keys -X begin-selection
bind-key -T copy-mode-vi d send-keys -X halfpage-down
bind-key -T copy-mode-vi u send-keys -X halfpage-up

# # Windows # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # #
bind-key -n M-n new-window -c '#{pane_current_path}'

# kill window
bind-key -n M-K confirm-before -p "kill-window? (y/n)" kill-window
bind-key -n M-x confirm-before -p "kill-pane? (y/n)" kill-pane

# go to numbered windows with meta key
bind-key -n M-1 select-window -t 1
bind-key -n M-2 select-window -t 2
bind-key -n M-3 select-window -t 3
bind-key -n M-4 select-window -t 4
bind-key -n M-5 select-window -t 5
bind-key -n M-6 select-window -t 6
bind-key -n M-7 select-window -t 7
bind-key -n M-8 select-window -t 8
bind-key -n M-9 select-window -t 9

# # Panes # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # #
# split vertically
bind-key -n M-\- split-window -v -c "#{pane_current_path}"
# even vertical
bind-key | select-layout even-horizontal

# split horizontally
bind-key -n M-\\ split-window -h -c "#{pane_current_path}"
# even horizontal
bind-key _ select-layout even-vertical

# left pane
bind-key -n M-h select-pane -R
# right pane
bind-key -n M-l select-pane -L
# up pane
bind-key -n M-k select-pane -U
# down pane
bind-key -n M-j select-pane -D

# # Pane Presets # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # #
# ┏━┳━┓
# ┃ ┣━┫ 3 grid of panes
# ┗━┻━┛
bind-key '3' source ~/.tmux/threePane
# ┏━┳━┓
# ┣━╋━┫ 4 grid of panes
# ┗━┻━┛
bind-key '4' source ~/.tmux/fourPane

# kill all panes but current
bind-key -n M-o "kill-pane -a; set-option pane-border-status"

